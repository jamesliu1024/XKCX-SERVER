<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seig.ljm.xkckserver.mapper.AccessDeviceMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="seig.ljm.xkckserver.entity.AccessDevice">
        <id column="device_id" property="deviceId" />
        <result column="location" property="location" />
        <result column="ip_address" property="ipAddress" />
        <result column="status" property="status" />
        <result column="device_type" property="deviceType" />
        <result column="description" property="description" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        device_id, location, ip_address, status, device_type, description
    </sql>

    <!-- 获取设备使用统计 -->
    <select id="getDeviceUsageStats" resultType="java.util.Map">
        SELECT 
            d.device_id,
            COUNT(l.log_id) as totalAccess,
            SUM(CASE WHEN l.result = 'allowed' THEN 1 ELSE 0 END) * 100.0 / COUNT(l.log_id) as successRate
        FROM 
            AccessDevice d
            LEFT JOIN AccessLog l ON d.device_id = l.device_id
        WHERE 
            d.device_id = #{deviceId}
        GROUP BY 
            d.device_id
    </select>

</mapper>
